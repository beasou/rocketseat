//Inferência de tipos]
    //reconhecer de forma automatica o tipo de variavel, baseado no conteudo atribuído na declaração

let user: string = 'Beatriz' //posso retirar a declaração feita de forma explicita

let useR = 'Beatriz'

useR = 10 //erro, pois em useR esta inferido o tipo string