/*
    O JavaScript é uma linguagem fracamente tipada e dinâmica,
    ou seja, ao declarar uma variável não é necessário estabelecer 
    um tipo específico e o tipo do valor da variável pode mudar conforme o código.

    -variaveis não precisam ter um tipo previamente definido
    -podemso mudar o conteúdo da variável
*/

    let clima = true;
    console.log(typeof clima);

    let clima = "";
    console.log(typeof clima);

    let clima = 0;
    console.log(typeof clima);
    
