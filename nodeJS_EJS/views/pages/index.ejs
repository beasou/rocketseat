<!DOCTYPE html>
<html lang="pt-br">
    
<!--importando os pedaços da pagina -->
    <%- include('./partials/head') %> <!--não precisa. ejs, pois ele entende q se trata desse arquivo-->

<body class= container>

    <%- include('./partials/header') %>

    <main>
        <div class="jumbotron">
            <h1>Node com EJS</h1>
            <p>Vamos aprender a trabalhar com arquivos EJS</p>
            <p><%= subtitle  %></p> <!--pegando o subtitle de server-->
        </div>
        <ul>
            <% qualitys.forEach(function(quality){%>
                <li>
                    <strong><%= quality.title %></strong> <%= quality.message %>
                </li>
            <% }) %>
        </ul>
    </main>

    <%- include('./partials/footer') %>

</body>
</html>